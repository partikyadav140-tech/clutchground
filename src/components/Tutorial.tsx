/**
 * Legacy Tutorial stub.
 *
 * The old driver.js tutorial has been replaced by the LiveTutorial system
 * (see LiveTutorial.tsx + tutorial-store.ts). This component is kept as a
 * no-op so any existing <Tutorial /> references don't break, but it does
 * nothing. The LiveTutorial is rendered in _app.tsx instead.
 */
export function Tutorial() {
  return null;
}
